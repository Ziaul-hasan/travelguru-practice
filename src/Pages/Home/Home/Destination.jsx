import React from 'react';

const Destination = ({destination}) => {
    const {description, name, image, id} = destination
    return (
        <div>
            
        </div>
    );
};

export default Destination;